//This module contains a template that creates and returns contact HTML components so that contactList can append them to the DOM.


//definitely has a function takes in an object and returns a document fragment (DOM component)

const contact = {
  makeDOMcomponentFromObject(contactObject) {
    console.log(contactObject);
    //return a document fragment with the info from the contactObject that got passed in to this function
  }
}

export default contact;